spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  flyway:
    enabled: false

# Foi preciso adicionar no arquivo de teste pois o código necessita essa adição quando subir a aplicação
broker:
  exchange:
    pedido:
      notificacao:
        event: ms.produto.notificacaoevent.teste
  key:
    pedido:
      notificacao:
        event: ms.produto.notificacaoevent.key.teste
  queue:
    pedido:
      notificacao:
        event: ms.produto.notificacaoevent.queue.teste